alertmanager:
  enabled: false
pushgateway:
  enabled: false
server:
  retention: 2h
  persistentVolume:
    size: 1Gi
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      memory: 512Mi
